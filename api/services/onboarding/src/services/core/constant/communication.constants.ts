export namespace Communication {

    export enum ChannelType {
        Sms = 'Sms',
        Email = 'Email'
    }

    export enum Vendor {
        Gupshup = 'Gupshup'
    }

    export enum MessageType {
        Otp = 'Otp'
    }
}
